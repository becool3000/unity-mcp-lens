# Telemetry

Telemetry is a core part of this project.

The goal is not just performance monitoring. The useful question is:

> How expensive is it for an agent to stay oriented and keep making progress?

---

## What Is Measured

Lens records several classes of rows:

- Tool execution payload rows.
- Bridge request and response rows.
- Tool snapshot rows.
- Pack transition rows.
- Coverage rows for TSAM stages.
- Detail-ref and shaped-output metadata when available.

For TSAM tools, the expected stage coverage is:

- `normalization`
- `service`
- `adapter`
- `result_shaping`

---

## Why Telemetry Matters

In agent-driven Unity work, cost includes more than runtime duration:

- token and context usage
- repeated schema requests
- reconnect/setup loops
- pack churn
- ambiguous results that trigger custom probes
- large editor-state or snapshot payloads
- missing tools that force `Unity.RunCommand` or raw file/log inspection

Telemetry should make those costs visible enough to choose the next fix.

---

## Current Dogfood Baseline

The 2026-04-24 dogfood run in `D:\2DUnityNewGame` produced these important
signals:

- `2,574` total usage rows.
- `1,134` bridge requests and `1,140` bridge responses.
- `34` bridge connections.
- `127` setup cycles.
- `184` pack-set transitions.
- `356` `get_tool_schema` requests.
- `6` unmatched bridge requests, all tied to `Unity_ManageEditor` transport closure during domain reload.
- `0.00%` recorded shaping savings on eligible payload rows.
- Largest payload rows were `Unity_ManageEditor` results above `220 KB`.
- Tool snapshots contributed about `2.50 MB` raw payload across `29` rows.

The same run confirmed that the exercised Phase 10 project tools emitted full
TSAM stage coverage:

- `Unity.InputSystem.Diagnostics`
- `Unity.ProjectSettings.PreviewActiveInputHandler`
- `Unity.ProjectSettings.SetActiveInputHandler`

## Phase 11 Smoke Baseline

A focused Phase 11 smoke test on 2026-04-24 passed with a residual payload-shaping warning:

- Host project: `D:\2DUnityNewGame`.
- Unity version: `6000.4.3f1`.
- Metadata audit: pass.
- Export counts: `foundation=12`, `foundation+scene=30`, `project=21`, `debug=22`.
- Compact rerun span: `44` rows, lines `1279..1324`.
- Bridge churn in compact rerun: `1` connection, `0` setup cycles, `0` unmatched requests.
- Phase 11 TSAM coverage: complete for package compatibility, input-actions inspection, diagnostics, preview, and set.
- Phase 11 failure classes: none.

Smoke notes:

- Compatibility signals now collapse repeated `Unity.InputSystem.IntegrationTests.dll` log-skip messages into one informational issue and keep overall status `ok`.
- The post-smoke usage report now excludes its own in-flight request and no longer flags the final `Unity_GetLensUsageReport` call as unmatched.
- Payload report still shows `NoShapingRecorded=true`.

---

## Current Findings

### Session And Schema Churn

Repeated setup cycles, connections, pack transitions, and schema requests are a
major source of control-plane noise. Helper scripts should reuse sessions and
avoid broad setup work when the editor status beacon and existing manifest are
fresh enough.

### Payload Shaping Gaps

Large `Unity.ManageEditor` and tool-snapshot rows show that compact output is
not yet consistently reducing recorded payload size. This may mean shaping is
missing expensive rows, or telemetry is recording the raw side of the operation
instead of the shaped side.

### Diagnostic Surface Gaps

Phase 11 closed the biggest package/import/Input System read-only gap, and the
compact compatibility view now keeps known benign repeated Input System
integration-test skip lines informational instead of escalating overall status.
The next TSAM diagnostics cluster should cover missing scripts, reference
validation, and import side effects with the same compact/read-first behavior.

### Result Ambiguity

`Unity.RunCommand` must keep failure stage, compilation state, execution state,
and logs internally consistent. Ambiguous stage metadata leads directly to
smaller manual probes and slower recovery.

---

## Definition Of A Good Telemetry Outcome

A good Lens change should usually improve at least one of these:

- fewer bridge connections per task
- fewer setup cycles
- fewer `get_tool_schema` calls
- fewer pack transitions
- smaller shaped payloads
- fewer unmatched requests outside expected reload/play transitions
- full TSAM stage coverage for split tools
- clearer failure classes and recovery hints
